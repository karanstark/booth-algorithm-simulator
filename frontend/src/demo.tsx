import { InteractiveNeuralVortex } from "@/components/ui/interactive-neural-vortex-background";

/** Optional demo wrapper for the neural vortex landing hero. */
export function DemoOne() {
  return <InteractiveNeuralVortex />;
}
